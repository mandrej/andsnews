#!/usr/bin/env bash
proj_id=andsnews
venv_dir=/home/milan/venv/andsnews3
version=`date +%Y%m%d%H%M`

function syntax {
  echo $"Usage:"
  echo $"$0 server  - Run development server"
  echo $"$0 build  - Build client"
  echo $"$0 deploy - Deploy"
  exit 1
}

if [ $# -eq 0 ]
  then
    syntax
else
  case $1 in
    server)
      source $venv_dir/bin/activate
      python main.py
      # vue ui
      exit ;;
    build)
      sed -i -E "/VUE_APP_VERSION/s/[[:digit:]]{12}/$version/g" vue2/.env
      sed -i -E "/assetsVersion/s/[[:digit:]]{12}/$version/g" vue2/vue.config.js
      yarn --cwd vue2/ build
      exit ;;
    deploy)
      gcloud app deploy --project=$proj_id --version=$version app.yaml index.yaml dos.yaml
      # gcloud datastore indexes cleanup index.yaml
      exit ;;
    *)
      syntax
  esac
fi
