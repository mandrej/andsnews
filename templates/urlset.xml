<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ uri_for('photo_all', _full=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>
    {% for object in photos %}
        <url>
            <loc>{{ uri_for('photo', slug=object.key.string_id(), _full=True) }}</loc>
            <lastmod>{{ object.date.strftime(full_date) }}Z</lastmod>
            <changefreq>monthly</changefreq>
            <priority>0.3</priority>
        </url>
    {% endfor %}
    <url>
        <loc>{{ uri_for('entry_all', _full=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>
    {% for object in entries %}
        <url>
            <loc>{{ uri_for('entry', slug=object.key.string_id(), _full=True) }}</loc>
            <lastmod>{{ object.date.strftime(full_date) }}Z</lastmod>
            <changefreq>monthly</changefreq>
            <priority>0.3</priority>
        </url>
    {% endfor %}
</urlset>