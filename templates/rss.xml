<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
    <channel>
        <title>{{ title }}</title>
        <link>{{ HOST }}{{ link }}</link>
        <lastBuildDate>{{ objects.0.date|format_datetime('%a, %d %b %Y %I:%M:%S %p GMT') }}</lastBuildDate>
        <description>{{ description }} ANDрејевићи</description>
        {% for item in objects %}
            <item>
                <title>{{ item.headline }}</title>
                {% if kind == 'photo' %}
                    <description>
                        Camera model: {{ item.model }},
                        Lens type: {{ item.lens }},
                        Aperture: F{{ item.aperture }},
                        Shutter speed: {{ item.shutter }} sec,
                        Focal length: {{ item.focal_length }} mm (~ {{ item.eqv }} eqv.),
                        Sensitivity (ISO): {{ item.iso }}
                    </description>
{#                    <enclosure url="{{ item.small_url() }}" length="" type="image/jpeg"/>#}
                {% else %}
                    <description>{{ item.summary }}</description>
                {% endif %}
                <link>{{ HOST }}{{ item.get_absolute_url() }}</link>
                <author>{{ item.author.email() }}</author>
                <pubDate>{{ item.date|format_datetime('%a, %d %b %Y %I:%M:%S %p GMT') }}</pubDate>
                <guid isPermaLink="true">{{ HOST }}{{ item.get_absolute_url() }}</guid>
                <category>{{ item.tags|join(', ') }}</category>
            </item>
        {% endfor %}
    </channel>
</rss>