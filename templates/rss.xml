<?xml version="1.0" encoding="UTF-8" ?>
{% macro _item(object, detail_name) %}
    <title>{{ object.headline }}</title>
    <link>{{ uri_for(detail_name, slug=object.key.string_id(), _full=True) }}</link>
    <author>{{ object.author.email() }}</author>
    <pubDate>{{ object.date|format_datetime(format) }}</pubDate>
    <guid isPermaLink="true">{{ uri_for(detail_name, slug=object.key.string_id(), _full=True) }}</guid>
    <category>{{ object.tags|join(', ') }}</category>
{% endmacro %}
<rss version="2.0">
    <channel>
        {% if kind == 'photo' %}
            <title>Photos</title>
            <link>{{ uri_for('photo_all', _full=True) }}</link>
            <lastBuildDate>{{ objects.0.date|format_datetime(format) }}</lastBuildDate>
            <description>Latest photos from the ANDрејевићи site</description>
            {% for item in objects %}
                <item>
                    {{ _item(item, kind) }}
                    <description>
                        Camera model: {{ item.model }},
                        Lens type: {{ item.lens }},
                        Aperture: F{{ item.aperture }},
                        Shutter speed: {{ item.shutter }} sec,
                        Focal length: {{ item.focal_length }} mm (~ {{ item.eqv }} eqv.),
                        Sensitivity (ISO): {{ item.iso }}
                    </description>
{#                    <enclosure url="{{ item.small_url() }}" length="" type="image/jpeg"/>#}
                </item>
            {% endfor %}
        {% else %}
            <title>Entries</title>
            <link>{{ uri_for('entry_all', _full=True) }}</link>
            <lastBuildDate>{{ objects.0.date|format_datetime(format) }}</lastBuildDate>
            <description>Latest entries from the ANDрејевићи site</description>
            {% for item in objects %}
                <item>
                    {{ _item(item, kind) }}
                    <description>{{ item.summary }}</description>
                </item>
            {% endfor %}
        {% endif %}
    </channel>
</rss>